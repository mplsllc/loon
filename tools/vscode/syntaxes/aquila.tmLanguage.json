{
  "scopeName": "source.aquila",
  "patterns": [
    {"include": "#comments"},
    {"include": "#keywords"},
    {"include": "#types"},
    {"include": "#strings"},
    {"include": "#numbers"},
    {"include": "#operators"},
    {"include": "#constructors"},
    {"include": "#functions"}
  ],
  "repository": {
    "comments": {
      "patterns": [{
        "name": "comment.line.double-slash.aquila",
        "match": "//.*$"
      }]
    },
    "keywords": {
      "patterns": [{
        "name": "keyword.control.aquila",
        "match": "\\b(fn|let|type|match|for|in|do|module|imports|exports|sequential|true|false)\\b"
      }, {
        "name": "keyword.operator.arrow.aquila",
        "match": "->|\\|>"
      }]
    },
    "types": {
      "patterns": [{
        "name": "entity.name.type.aquila",
        "match": "\\b(Int|Bool|String|Float|Unit|Array|Option|Result)\\b"
      }, {
        "name": "storage.modifier.effect.aquila",
        "match": "\\[IO\\]|\\[\\]"
      }]
    },
    "strings": {
      "patterns": [{
        "name": "string.quoted.double.aquila",
        "begin": "\"",
        "end": "\"",
        "patterns": [{"name": "constant.character.escape.aquila", "match": "\\\\."}]
      }]
    },
    "numbers": {
      "patterns": [{
        "name": "constant.numeric.float.aquila",
        "match": "\\b[0-9]+\\.[0-9]+\\b"
      }, {
        "name": "constant.numeric.integer.aquila",
        "match": "\\b[0-9]+\\b"
      }]
    },
    "operators": {
      "patterns": [{
        "name": "keyword.operator.aquila",
        "match": "==|!=|<=|>=|&&|\\|\\||[+\\-*/%<>=!]"
      }]
    },
    "constructors": {
      "patterns": [{
        "name": "entity.name.tag.aquila",
        "match": "\\b[A-Z][a-zA-Z0-9_]*\\b"
      }]
    },
    "functions": {
      "patterns": [{
        "name": "entity.name.function.aquila",
        "match": "\\b([a-z_][a-zA-Z0-9_]*)\\s*(?=\\()"
      }]
    }
  }
}
