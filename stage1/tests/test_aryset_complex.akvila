module test;
fn compute(x: Int) [] -> Int { x * 2 }
fn main() [IO] -> Unit {
    let arr: Array<Int> = Array(4);
    arr[0] = compute(5);
    arr[1] = compute(arr[0]);
    arr[2] = match arr[1] == 20 { true -> compute(arr[1]), false -> 0 };
    do exit(arr[2]);
}
