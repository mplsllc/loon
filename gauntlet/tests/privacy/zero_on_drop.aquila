// TEST: zero_on_drop
// EXPECT: COMPILES OK, exit 0
// CATEGORY: privacy
module test;
fn use_secret() [IO] -> Unit {
    let pw: Sensitive<String, ZeroOnDrop> = "hunter2";
    do print("processing");
}
fn main() [IO] -> Unit {
    do use_secret();
    do exit(0);
}
