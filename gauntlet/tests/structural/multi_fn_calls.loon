// TEST: multi_fn_calls
// EXPECT: COMPILES OK, exit 32
// CATEGORY: structural
module test;
fn step1(x: Int) [] -> Int { x + 10 }
fn step2(x: Int) [] -> Int { x * 2 }
fn step3(x: Int) [] -> Int { x + 2 }
fn main() [IO] -> Unit {
    let r: Int = step3(step2(step1(5)));
    do exit(r);
}
