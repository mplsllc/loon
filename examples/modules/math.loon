module math;

exports [abs, max]

fn abs(x: Int) [] -> Int {
    match x >= 0 {
        true -> x,
        false -> 0 - x,
    }
}

fn max(a: Int, b: Int) [] -> Int {
    match a >= b {
        true -> a,
        false -> b,
    }
}
